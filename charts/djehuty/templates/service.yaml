apiVersion: v1
kind: Service
metadata:
  name: {{ include "djehuty.fullname" . }}
  labels: {{- include "djehuty.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "djehuty.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
